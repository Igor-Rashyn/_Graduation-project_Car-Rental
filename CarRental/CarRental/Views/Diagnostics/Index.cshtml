@model PagedList.IPagedList<CarRental.Models.Diagnostics>
@using PagedList.Mvc

<link href="~/Content/PagedList.css" rel="stylesheet" type="text/css" />

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<br />
<br />
<br/>
<p>
    @Html.ActionLink(ResDiagnostics.Diagnostics_Index.CreateNew, "Create")
</p>

<table class="table table-striped table-hover">
    <thead>
        <tr>
            <th>@ResDiagnostics.Diagnostics_Index.Application</th>
            <th>@ResDiagnostics.Diagnostics_Index.FirstName</th>
            <th>@ResDiagnostics.Diagnostics_Index.LastName</th>
            <th>@ResDiagnostics.Diagnostics_Index.Email</th>
            <th>@ResDiagnostics.Diagnostics_Index.Phone</th>
            <th>@ResDiagnostics.Diagnostics_Index.Car</th>
            <th>@ResDiagnostics.Diagnostics_Index.Model</th>
            <th>@ResDiagnostics.Diagnostics_Index.TotalPrice</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(m => item.Order.ApplicationNumber)</td>
                <td>@Html.DisplayFor(m => item.Order.FirstName)</td>
                <td>@Html.DisplayFor(m => item.Order.LastName)</td>
                <td>@Html.DisplayFor(m => item.Order.Email)</td>
                <td>@Html.DisplayFor(m => item.Order.Phone)</td>
                <td>@Html.DisplayFor(m => item.Car.CarNumber)</td>
                <td>@Html.DisplayFor(m => item.Car.Brand) @Html.DisplayFor(m => item.Car.Model)</td>
                <td>@Html.DisplayFor(m => item.Order.TotalPrice)</td>
                <td>
                    @Html.ActionLink(ResDiagnostics.Diagnostics_Index.Detail, "Detail", new { id = item.Id })
                </td>
            </tr>
        }
    </tbody>
</table>

<br />

        Page @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) of @Model.PageCount

        @Html.PagedListPager(Model, page => Url.Action("Index", new { page }))
